# Classic Romance Hindi Dialogue Training Upload

Use `train_all_movies.jsonl` for the full dataset. Each row has relative `audio` and `voice_prompt` paths rooted at this folder.

Per-movie copies are under `movies/<movie_id>/` with their own clips, prompts, manifest, and summary.
