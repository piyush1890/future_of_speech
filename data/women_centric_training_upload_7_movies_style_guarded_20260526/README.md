# Women-Centric Hindi Dialogue Training Upload

Use `train_all_movies.jsonl` for the full dataset. Each row has relative `audio` and `voice_prompt` paths rooted at this folder.

Per-movie copies live under `movies/<movie_id>/` with target clips, voice prompts, and small manifests only. Rejected clips, source videos, intermediate model outputs, and review folders are intentionally excluded.
